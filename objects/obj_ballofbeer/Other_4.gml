var _off = 12
if place_meeting(x, y - _off, obj_doorA) || place_meeting(x, y + _off, obj_doorA)
    targetDoor = "A"
if place_meeting(x, y - _off, obj_doorB) || place_meeting(x, y + _off, obj_doorB)
    targetDoor = "B"
if place_meeting(x, y - _off, obj_doorC) || place_meeting(x, y + _off, obj_doorC)
    targetDoor = "C"
if place_meeting(x, y - _off, obj_doorD) || place_meeting(x, y + _off, obj_doorD)
    targetDoor = "D"
if place_meeting(x, y - _off, obj_doorE) || place_meeting(x, y + _off, obj_doorE)
    targetDoor = "E"
if place_meeting(x, y - _off, obj_doorF) || place_meeting(x, y + _off, obj_doorF)
    targetDoor = "F"
if place_meeting(x, y - _off, obj_doorG) || place_meeting(x, y + _off, obj_doorG)
    targetDoor = "G"
if place_meeting(x, y - _off, obj_doorH) || place_meeting(x, y + _off, obj_doorH)
    targetDoor = "H"
if place_meeting(x, y - _off, obj_doorS) || place_meeting(x, y + _off, obj_doorS)
    targetDoor = "S"